package com.juffyto.juffyto.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales
val Primary = Color(0xFF0D47A1)         // Azul oscuro principal
val PrimaryVariant = Color(0xFF1976D2)   // Azul principal variante
val Secondary = Color(0xFF2196F3)        // Azul secundario
val Accent = Color(0xFF64B5F6)          // Azul claro para acentos
val Tertiary = Color(0xFF90CAF9)        // Azul muy claro

// Modo Claro
val LightBackground = Color(0xFFF8F9FA)  // Fondo principal claro
val LightSurface = Color(0xFFFFFFFF)     // Superficie de cards y elementos
val LightSurfaceVariant = Color(0xFFF5F5F5) // Superficie secundaria
val LightTextPrimary = Color(0xFF1F1F1F)    // Texto principal
val LightTextSecondary = Color(0xFF616161)  // Texto secundario
val LightTextTertiary = Color(0xFF9E9E9E)   // Texto terciario/deshabilitado

// Modo Oscuro
val DarkBackground = Color(0xFF121212)     // Fondo principal oscuro
val DarkSurface = Color(0xFF1E1E1E)        // Superficie de cards y elementos
val DarkSurfaceVariant = Color(0xFF2D2D2D) // Superficie secundaria
val DarkTextPrimary = Color(0xFFE1E1E1)    // Texto principal
val DarkTextSecondary = Color(0xFFB0B0B0)  // Texto secundario
val DarkTextTertiary = Color(0xFF808080)   // Texto terciario/deshabilitado

// Estados y Funcionalidades
val Success = Color(0xFF4CAF50)      // Verde para éxito
val Warning = Color(0xFFFFA726)      // Naranja para advertencias
val Error = Color(0xFFE53935)        // Rojo para errores
val InProgress = Color(0xFF29B6F6)   // Azul claro para "en progreso"
val Completed = Color(0xFF66BB6A)    // Verde para "completado"
val Upcoming = Color(0xFF9575CD)     // Morado para "próximo"

// Colores para bordes y divisores
val LightDivider = Color(0xFFE0E0E0)
val DarkDivider = Color(0xFF424242)